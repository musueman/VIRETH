# Prototype Instructions

## Favicon and link preview · 2026-09-17

- Favicon only: two mirrored hot-pink diagonal strokes forming a symmetric V on opaque white. Keep existing header/logo artwork unchanged. Editable source is `public/assets/branding/favicon-v1.svg`; `scripts/build-favicon.cjs` exports PNG sizes and ICO with sharp.
- Share-card correction: use the exact original PNG logo, not an AI-redrawn wordmark. Center it prominently (720px wide on the 1200px card), with a large centered 44px slogan below and a dark readability gradient over the original night-city background. Rebuild with `scripts/build-share-card.cjs`. The generated left-biased trials were rejected; do not use them as live artwork.
- Use one site-wide, crawler-visible static Open Graph/Twitter preview in index.html: existing night-city/two-moon scenery with VIRETH branding and “누구든, 무엇이든 될 수 있는 곳”. Title: “VIRETH · 당신의 이야기가 시작되는 세계”. Description: “성문 앞에서, 낯선 장터에서, 누군가의 곁에서. 나라와 사람들을 만나고 당신만의 여행을 시작하세요.” Use absolute public image/canonical URLs, Pages-safe icon paths, and a 1200×630 PNG. Hash routes share this preview; do not imply per-story social cards.

## Compact music footer and stacked map · 2026-09-16

- Replace footer branding with the three supplied BGM tracks and a compact player. Attempt audible autoplay on initial entry; if browser policy blocks it, retry on the first page click/tap or Enter/Space activation. Never auto-resume after explicit pause, and do not show policy-blocked initial autoplay as an error. Keep one audio element across hash routes, repeat the three-track playlist, preserve pause on manual selection, and use a genuine Web Audio analyser for the equalizer. Pause visual sampling in hidden tabs/offscreen footer/reduced motion; do not use fake dancing bars. Retain original audio files and embedded track titles.
- At widths <=1000px, remove footer navigation and branding from the rendered layout, use a 56px controls row plus bottom safe-area inset, and keep smaller Duran/Bobo above the right edge without covering controls. Wider layouts keep footer navigation and desktop player controls. Preserve the fixed white footer, shared header reveal boundary and measured footer obstruction.
- The homepage map becomes stacked at the existing 1000px breakpoint. In this layout ONLY, keep the selected country card in normal document flow 20px beneath the main map, never in a fixed portal. Put a thin native country/region selector directly above the map. Click/tap/Enter/Space/select change scrolls to the card below the header; hover/focus alone does not scroll. Resizing clears pending floating motion. Wider two-column/overlapping layouts retain all existing floating-card, dismissal and stationary-pointer protections. Preserve maps, canonical coordinates and every country.

## Full-screen travel menu · 2026-09-16

- User approved implementing the planned warm-white travel-directory hamburger menu, with a full-viewport unfolding surface and short staggered content entrances. All viewport sizes now expose the menu toggle; desktop direct links remain. Use gothic UI labels, navy type, muted-gold separators and the unchanged faint monochrome logo.
- The latest layer request supersedes hiding the footer: portal FullMenu directly to body at z-index 14, below the existing footer/companions (15) and header (20), above page cards (12). Keep the footer visibly in front. Its background controls are inert while the menu is active. The menu scroll viewport ends at the measured footer obstruction; do not put the menu inside the header stacking context.
- Preserve six guide destinations, category/start/utility routes and the verified LunaTalk introduction link. Mobile guide details start collapsed except on guide pages. Keep scroll lock, focus wrap/Escape/restore, route-change cleanup, safe search handoff and cancellation of stale close timers. Reduced motion skips entrance/exit animations. Do not animate text continuously.

## Responsive corrections · 2026-09-16

- Map destination CTA: keep its text on one line and the 18px arrow against the right 16px inner padding. Scope map-wide SVG sizing to `.detail-map > svg`, never descendant button icons; preserve the 44px target and current font size.
- Keep body copy and type sizes intact when fixing mobile overflow/blank space. The newcomer CTA stays one line through internal spacing, not smaller type. Person details introduce name/role before portrait in reading order; desktop/tablet still place the portrait beside the information, with intrinsic-ratio height instead of a tall empty sticky box.
- Place-detail mobile headers are content-height, with a 44px body-to-map transition rather than a 340px minimum header. Compact only the affected mobile guide/related-section gaps. Preserve home map dismissal slot geometry/watermark and fixed footer behavior.
- Detail maps retain original small dots and canonical coordinates. A 44px native place picker and explicit destination link provide a non-overlapping alternative to tiny crowded markers; retain all nearby destinations and reset selection on route changes.
- Full hamburger-menu design is recorded in `docs/full-menu-design-plan.md`; the later full-screen menu approval above supersedes its original planning-only status.

## Empty exploration card watermark · 2026-09-16

- Keep the existing VIRETH logo as a faint monochrome watermark in the reserved country-card slot, so dismissal leaves a deliberate background rather than an empty navy panel. Use the unchanged branding PNG with CSS brightness(0) invert(1), opacity .085, natural aspect ratio and no box. On desktop confine it to the left 40% column (the card slot is wider at 48%); on stacked mobile use the slot width.
- Watermark is decorative, pointer-inert and static behind the inline card; it does not participate in card selection, reveal animation, portal position or slot-height calculations. Preserve dismissed-state behavior and all map geometry.

## Header LunaTalk entry · 2026-09-16

- Keep the official LunaTalk favicon as an icon-only 44px outbound link immediately after search, available on desktop/mobile and all routes. Use the existing project destination https://lunatalk.chat/character/detail/70170 in a new tab with noopener/noreferrer; it is the chatbot introduction entry, not a personal chat session, and may require login/adult authentication.
- Preserve the downloaded icon pixels; invert via CSS on the dark header and retain black on the white header. Provide an accessible new-tab label and hover/keyboard tooltip. At 681–900px collapse navigation into the existing mobile menu rather than shrinking wording or colliding with the new icon.

## Homepage welcome portraits · 2026-09-16

- Place the approved Duran cutout at the left and already-mirrored Maren at the right of #welcome. Full-body display is unnecessary: use a clipped, non-interactive absolute stage with broad bottom fading. Never horizontally flip Maren a second time. Preserve the night-city art, heading, body, font sizes and CTA destinations.
- `WelcomeCharacters.jsx` / `welcome-characters.css` keep the central reading corridor clear. Mobile places the portraits below the unchanged copy/CTA group and uses a 900px minimum hero for the two zones. Preserve original PNGs, intrinsic aspect ratios, Reveal entrance/replay and reduced motion. Do not affect the existing newcomer Duran or footer sprites.

- 2026-09-16 full-width catalog backgrounds: category hero art must reach both viewport edges even above 1920px, while copy/filter gutters stay unchanged. The old child-relative --content-edge calculation left 153px side gaps at 1920. Use `.category-overview` as an inline-size query container and `100cqw` width centered on the introduction for catalog/regions art; this excludes the scrollbar and avoids horizontal overflow. Catalog backgrounds receive a subtle 125-degree diagonal screen in a separate pseudo-layer, never over text/filter UI. Preserve gradients and original assets; guide right-column fixed backgrounds are a separate layout and stay unchanged.

## Six guide-page fixed backgrounds · 2026-09-16

- User requested distinct viewport-fixed background artwork for all six guide pages, separate from the left menu. `Reader.jsx` maps stable guide ids to existing decorative art: first/night city, daily/inn conversation, places/mountain route, roads/ferry, promises/contract table, history/archive. These are thematic illustrations, not asserted exact historical events or named characters.
- `guide-background.css` scopes a non-interactive fixed image behind the right reading column, below header and above footer obstruction, with broad navy readability gradients. The sidebar remains plain; mobile keeps its menu on an opaque navy surface and the reading background fixed. Preserve all wording, font sizes, inline scene figures, menu selection, next/back links and reduced-motion Reveal behavior. No new generated art or canon changes.

- 2026-09-16 footer companion spacing: move only Duran 28px toward Bobo on desktop and 16px on mobile, preserving sprite sizes, foot baseline and Bobo's walking path. Duran sits at z-index 1 and Bobo's entire moving/facing group at z-index 2 so Bobo stays in front when overlapping. Preserve navigation and motion controls.

## Catalog overview backgrounds · 2026-09-16

- User approved background images in the /stories and /people overview headers. Reuse approved journey art: `v1/25_ledger-clue.webp` for records/stories and `v1/30_market-view.webp` for everyday people. These are decorative possible encounters, not portraits of named canonical characters. Keep source pixels unchanged.
- `CatalogIntroduction` and scoped `.category-overview .catalog-introduction` styles preserve existing copy, type sizes, gutters, header height, filters and cards. Use broad warm-ivory left readability gradients and a soft bottom fade; mobile has stronger ivory coverage. Keep Picture/Reveal and reduced-motion support. Do not propagate to story/person details or other categories without approval.

## Place-detail panorama backgrounds · 2026-09-16

- User approved extending upper gradient backgrounds to all 166 city/village/outpost detail routes. This supersedes the earlier exclusion of place-detail headers only; do not restyle people/stories/other overview pages as part of it.
- `src/place-art.json` maps stable place ids to optimized original approved panoramas in `public/assets/places/`. Rebuild with `node scripts/sync-place-art.mjs` (or `--check` for mapping-only validation). The historical review index resolves Korean names, but each asset must also appear in the current active image-mapping DB and exist under 00_최신본. Only explicit alias: 라드바르할 / 실버킵 → 라드바르할. Reject missing/ambiguous mappings; never silently substitute a regional capital.
- `src/place-detail.css` scopes all changes under `.place-page`: existing-size title/copy on a readable left-to-right dark navy gradient and broad bottom fade. Use original images and existing Reveal with responsive cover crop. Retain map geometry, text, representative-region figure/caption and navigation. Originals remain untouched; provenance/hashes live in `qa/place-backgrounds/provenance.json`.

## Countries overview banner and heraldry · 2026-09-16

- Approved background: `qa/regions-hero-v1/regions-introduction-background-v1.png`; live optimized derivative `public/assets/explore/regions-introduction-background-v1.webp`. Use only in the /regions introduction behind existing copy, with soft bottom blending. It is decorative, not canonical geography. Mobile uses a quiet 30% filtered image layer independent of Reveal opacity. Keep all typography, filter behavior and other route designs unchanged.
- On country overview cards, display each existing `assets/explore/crests/{id}.png` above its name and kind. Preserve original pixels and intrinsic proportions with contain sizing in a 72×96 desktop / 60×80 mobile slot, aligned to card copy. Do not regenerate heraldry or add a frame/shadow.

## Peer category hierarchy · 2026-09-16

- Extend approved hierarchy 3 only to the four peer overview routes: /regions, /stories, /people and /start without an id. Scope through `.category-overview` and `src/category-overview.css`: warm-white page, short muted-gold headline underline, distinct white filter surface, white card copy with consistent padding, fine gold top rules, UI-gothic entry names and controls. Preserve all existing text, type sizes, images, filtering, links and Reveal behavior. Keep country/place/person/story/start/guide details, utilities and homepage unchanged. Start overview illustrations sit above readable white descriptions rather than text over dark images. Do not extend this treatment to lower-level pages without separate approval.

## Approved section hierarchy · 2026-09-16

- User selected the third displayed hierarchy mock, archived as `qa/region-hierarchy-approved-3.png`. Keep the current country-detail layout, map geography, assets, all copy and type sizes. Give the light-section H2s a short 44px muted-gold underline, distinct spacing to explanatory body, and place the destination directory on one white grouped surface with a fine gold top rule, generous equal padding and subtle row/column separators. Destination names use the body gothic UI family to distinguish them from editorial serif section headings; CTA labels remain gothic. Align each row's CTAs at the bottom. Do not reproduce the mock's distorted map, misspellings, or footer sprites baked into its map. Preserve existing live map/sprites, reveal and hover/focus behavior. Mobile becomes one column with padding retained; no font reduction or text trimming. Scope this refinement to `.region-detail`.

## Approved Tiris riverbank correction · 2026-09-16

- Supersedes the blanket freeze of settlement anchors for two places only: Greenhollow moves from [551,799] to [500,838] on the inside of the southwest Tiris boundary near the river; its reclamation village Hekgarga moves from [540,813] to [512,825]. Use `src/approved-place-corrections.json` when rebuilding content. Preserve terrain bytes, borders, every other place, Ravenstone [503,756] and Newhollow [524,771]. Newhollow is a separate refugee settlement southeast of Ravenstone, not its alias or Greenhollow's satellite. Exact military landing/advance routes remain unspecified. Canon/LunaTalk and official map release updates are coordinated with task 019f412c-6407-7532-9d7e-3da903e9914b; remove the correction layer only after validating the replacement source and preserving its audit trail.

## Approved country detail · 2026-09-16

- Implement displayed mock 1 (`qa/region-detail-approved.png`) for all 20 country/region detail routes: full-width scenic hero and unboxed crest, followed by warm-white destination highlights, a full place directory with the canonical interactive map, then related people. Preserve existing canon copy, header/footer and replayable Reveal; no fabricated stats, quotes or city illustrations.
- The first two destinations have functional detail links. Only a place whose name matches the canonical capital/representative-place name may use that panorama as its own thumbnail. Other thumbnails show their actual v63 terrain location, explicitly labeled 지도 속 위치. Never depict a generated sample city as a different canonical city.
- Map CTA scrolls/focuses the current region's map below the header without changing the hash route. Honor reduced motion. All places remain reachable after the highlight row; non-sovereign regions retain no national-border shape. Scope styles to `.region-detail` so other page types stay unchanged until separately approved.

## Approved motion and header feedback · 2026-09-15

- 2026-09-16 country-card dismissal correction: after the first floating selection, retain its reserved slot but NEVER remount the inline card on dismissal. Keep it absent until deliberate pointer movement/focus/click selects again; stationary-pointer scrolling must not reopen it. Inactive country panels are absolute so only the active panel defines card height; preserve crossfade inside a clipped white panel container and make the outer card white too.
- 2026-09-16 people viewport fit: remove all three name-tab buttons. Keep all wording and font sizes, five-second rotation, portrait layers, video and pause control. On desktop size the section to viewport minus header/footer (640px content safety minimum), use 24px vertical padding, 18px copy-group gaps and 20px vertical profile padding; portraits fit the resulting column height instead of imposing 680px. The approved 1861×894 viewport fits the section at 727px without type reduction. Mobile keeps its readable stacked layout rather than clipping content into a fixed height.

- 2026-09-16 fixed footer revision supersedes the inline footer placement: enlarge both companion sprites to 130% of their previous desktop/mobile sizes, preserving the foot baseline. Order is brand → footer navigation → companions, with 24px separation after “처음으로”. Keep white desktop footer 85px high and dock to viewport bottom. Share `useSolidChrome` with header (homepage scrollY >24; internal pages always solid); slide the entire footer up/down over 450ms, below header/dialog layers. Hidden footer is inert and pauses sprites. Measure actual height with ResizeObserver, reserve a bottom spacer, expose --footer-obstruction, and lift country cards 24px above the footer. Reduced motion skips the slide. On mobile use brand/navigation left and companions right; hide only the short brand tagline to avoid overlap.

- 2026-09-16 compact white footer: white background, navy wordmark/dark-gray text; about 85px desktop height (20px block padding). Small original animated Duran/Bobo stand on the footer text baseline and protrude upward across its top edge, not in a new tall activity row. Reserve their own horizontal space so they never cover links; mobile shrinks them and puts navigation on a second row. Use source-derived transparent sprite atlases in assets/footer, keeping originals and provenance. Duran alternates idle/smile; Bobo walks a short distance, sniffs and returns. Preserve reveal, manual pause, reduced-motion still poses and offscreen/hidden-tab frame suspension.

- 2026-09-16 slider input recovery: StoryRibbon and JourneyGallery share input-modality tracking. Pause for hover and keyboard focus, not residual focus from pointer clicks. Preserve explicit user pause independently. Block native artwork dragging and suppress drag-release clicks after movement beyond 8px, but allow the next ordinary click. Clean up pointer end/cancel/window blur and Journey touchcancel; keep mobile native scrolling, keyboard access, reduced motion and existing crossfades unchanged.

- 2026-09-16 archive ribbon placement correction supersedes the full-width bottom row: the ribbon belongs INSIDE the left archive column, beneath its introduction and over the pictured book area, alongside the right story list. Keep both columns in one desktop grid row with balanced vertical bounds; do not append a new full-width section row or increase section height solely for the ribbon. On mobile, reading order is introduction → ribbon → featured stories. Preserve all pause/link/reduced-motion behavior.

- 2026-09-16 light country-card body: retain original capital/crest art, padding, location and transitions, but use white for `.place-details`, deep navy heading, dark gray body and dark-gold metadata/link. Avoid making every section/surface the same navy; introduce deliberate light/dark contrast without recoloring all sections indiscriminately.
- 2026-09-16 archive story ribbon: show all 30 existing story illustrations linked to their 14 actual story routes in a slow seamless horizontal loop below the archive content. Fade the viewport edges, reveal story titles on hover/focus, pause movement on hover/focus/manual pause/offscreen/hidden tab. Duplicate loop content must not duplicate keyboard or screen-reader stops. Mobile and reduced motion use a manually scrollable single copy with titles visible. Preserve existing archive artwork and candlelight.

- 2026-09-16 journey crossfade correction: incoming and outgoing images each need explicit opacity keyframes over 1600ms; never rely on an opacity transition after removing the incoming animation, since it can drop the outgoing image immediately to zero. Retain outgoing artwork for the animation plus 100ms cleanup margin. Keep six-second auto-advance, image preloading, masks and reduced-motion behavior.

- 2026-09-16 journey slideshow: preserve the left invitation and night-city background, with a soft-edged 16:9 gallery on the right (stacked below on mobile). Use 30 locally generated ComfyUI scenes, predominantly active encounters, cooperation, investigations and travel rather than static setting sheets. Illustrations are possible experiences, not new canonical events or named-character replacements. Auto-advance every 6 seconds with crossfade; pause on user request, hover/focus, offscreen, hidden tab and reduced motion. Keep previous/next, touch swipe, counter and accessible 44px controls. Audit anatomy, object connections and caption/action consistency before selecting generated images; keep originals and rejected revisions.

- 2026-09-16 button typography: all button/control labels use the body's Noto Sans KR gothic/sans-serif via --font-ui, including primary/secondary CTA links, text actions, tabs, search and form controls. Preserve existing sizes, padding, colors and effects. Keep Nanum Myeongjo for editorial headings and the decorative wordmark, not button labels.

- 2026-09-16 reveal/hover collision correction: entrance uses its own `reveal-enter` animation, never replaces component hover transitions. On completion move to `data-reveal=settled`, release animation and set translate:none so text does not retain an entrance transform. Match animationend only on the element itself; use a canceled-on-exit fallback, preserve re-entry replay and reduced motion. Keyboard focus settles immediately without disabling the button's 280ms hover/focus transition.

- 2026-09-16 universal entrance rule: every visual content object needs the established replayable 24px/700ms slide-fade-up. In archive, people and journey intros, reveal eyebrow/title/body/CTA separately with short stagger, not the entire copy block at once. Include scenic artwork, video layer, profile band, shared Picture/Link/Back, and footer content. Shared Reveal merges geometry styles with its delay and leaves nested artwork static when an ancestor already reveals the whole card; never double the translation. Preserve reduced-motion and keyboard-focus visibility. Fixed header and interactive country portal keep their existing specialized behavior.

- 2026-09-16 archive candlelight: preserve the original archive illustration. Add independently drifting warm candle glows and broad screen-blended reflections clipped to each paper page; never animate the artwork itself or illuminate UI copy/cards. Initial light was too faint, but stronger periodic fades felt like a dimmer: use continuous seeded smooth noise at multiple time scales, independent per candle, with quicker core-light sway/height changes and steadier outer halos. Paper/brass reflections also vary nonperiodically. Keep light coordinates on the same 1536×1024 cover-cropped plane as the source, including mobile 24% horizontal positioning. Pause frame work offscreen/hidden-tab and keep still under reduced motion.

- 2026-09-16 soft hover ends: mask only the background/border, never the text. Use a broad five-stop falloff: profile 256px, archive rows 128px, both 64px on mobile. Extend the background outward to preserve the bright reading surface behind text. Each homepage archive story independently brightens and changes all text to black on hover/focus over 450ms, with inner padding. Preserve source artwork and actual story links.

- 2026-09-16 profile hover: brighten the person-profile-band background to near-white and smoothly invert its label, name, body and detail link to black over 450ms. Match keyboard focus-within; preserve portrait stacking and layout. Reduced-motion removes the transition.

- 2026-09-16 people spacing correction supersedes the 3-second/absolute portrait layout: rotate every 5 seconds. Use separate grid columns for copy and portrait stage, stacked on mobile. Profile copy has 36px desktop / 28px tablet / 24px mobile inner padding. Only the profile background extends beneath the portrait stage; text must not. Clip slide animation within its own stage and keep its z-index above the profile background.

- 2026-09-16 follow-up: newcomer CTA and encyclopedia CTA are full buttons. City-edge art belongs to first-steps and sits at its actual bottom, not a fixed negative offset from hero. Header logo/search/rule align to shared --content-edge. Show two THE WORLD-pink diagonal stripes to the right of the header logo only on its white/scrolled state.
- People feature: rotate the three existing approved featured portraits every 3 seconds; selected enters from right with fade, outgoing fades away and returns behind, next waits darkened, rearmost is a monochrome silhouette. Keep clear profile separators and an encyclopedia button. Use user's 비레스20260819.mp4 as a muted darkened background with diagonal scanlines, preserving original; optimized derivative is people-background-v1.mp4. Pause automatic rotation/video off-screen, on hidden tab, with reduced motion and with user pause; keyboard focus pauses reading transitions. Preserve original portrait assets.

- 2026-09-16 newcomer light-theme revision: only the first-steps/Duran introduction section uses a white background, dark navy slogan, dark gray body, muted gray oversized logo and white readability/bottom gradients. Preserve logo source and 0.30 layer opacity, Duran reflection/crop, city-edge artwork, spacing and reveal. Intro link needs dark-gold contrast and a dark hover color on white. Other sections retain navy.

- 2026-09-16 territory/power clarification: preserve every v63 terrain pixel, border and anchor. Country power is not a ranking of polygon area. Leonia's court/knight/fief influence and Norghard's navy/ports/trade support great-power status; Tiris is a broad agrarian middle kingdom, not a tiny territorial state. City-state describes concentration around a core city, not foreign dependence. Homepage copy stays warm and simple; canon and LunaTalk wording is maintained in task 019f412c-6407-7532-9d7e-3da903e9914b. Do not invent population, military or measured-area ratios.

- 2026-09-16 v63 integration supersedes ALL older v62 map/coordinate requirements below. Use `src/geography-v63.json` generated by `scripts/sync-map-v63.mjs` from active `02_지도/16_지리확정_지도_v63`. The unchanged terrain is `public/assets/explore/v63/terrain.png`. Use its 1280×1920 coordinate system for all 20 representative anchors and all 15 country shapes; ignore legacy x/y in countries.json (art/copy metadata only). Five non-sovereign regions get representative markers, never fabricated national borders. Country polygon hover/focus/tap/Enter/Space and flags share the existing card handlers, including the stationary-pointer guard. Preserve capital/crest assets, spacing, viewport card motion and 56px clamps. The mobile enlarged atlas uses the same v63 terrain/borders and readable region labels. Canon/LunaTalk edits belong to task 019f412c-6407-7532-9d7e-3da903e9914b, not this homepage checkout.

- Card upper exit boundary: on scroll, when the floating card's top reaches or crosses `atlas viewport top + 56px`, dismiss it. Keep bottom-following and the lower stop. Enter with 320ms upward fade (12px to 0); exit from its captured current screen position with a 320ms upward fade (0 to -16px). Freeze positioning during exit; remove the portal only after animation completion, with a cleanup-safe fallback. Close/Escape/section exit share the motion; reduced motion dismisses immediately. Intentional new selection cancels any pending exit, preventing stale timers from hiding the new card.

- Latest card Y correction supersedes the header-adjacent top anchor: float 24px ABOVE THE VIEWPORT BOTTOM, but clamp the card bottom at 56px above the actual atlas bottom when scrolling reaches it. Compute bottom inset as `max(24, viewportHeight - mapViewportBottom + 56)` on selection, scroll and resize. Remain viewport-fixed/body-portaled, below the menu, with shared X alignment and stationary-pointer scroll guard. Do not return to a section-absolute Y anchor.

- Viewport-relative country cards (2026-09-16) supersede the previous bottom-anchored-only interaction: on hover, intentional mouse movement, focus, tap or list choice, portal the ONE detail card to body with `position: fixed`. Y belongs to the viewport (header height + 24px), never section/document coordinates; X follows the shared section content edge. Card z-index 12 < header 20 < notice 40. Constrain height to viewport with internal scrolling, preserve its inline slot to avoid page jumps, dismiss on section exit, close button or Escape, and restore focus without reopening. A scroll event blocks stationary-pointer hover selection until genuine mouse movement; explicit focus/click/list selection still works. Shared section gutters and vertical spacing keep newcomer/explore/card left edges aligned, including wide centered layouts and mobile. Preserve all current imagery.

- Full catalogue / bare heraldry update: include all 20 canonical countries and regions (15 political countries, 5 explicitly distinguished regions). Use `src/countries.json` and `public/assets/explore/all-country-provenance.json`. User expressly approved original-pixel-preserving background removal: cutouts change alpha and crop only, never redraw heraldry; keep originals. No CSS box/border/shadow around the crest. Normalize actual silhouette width to 96px desktop / 76px mobile with natural height and the same left inset as text. Country details remain boxed. v3 Leonia and v2 no-runes Hesferet override v1; Silnimet uses current central city Dorsorsan, not the older representative Silmermar vista. All selectors retain v62 mapped anchors. Show selected/hovered labels, turn eastern labels inward, and offer a grouped native list for access to all 20. Keep at least 40px between the heading/list and information panel. Verify assets with `scripts/verify-country-assets.py`.

- Country-first correction (2026-09-16) supersedes city-first H3: map selectors name COUNTRIES, not cities. The box shows country name and short plain-language national introduction, a verified capital/central-city panorama, and its current heraldry. Align crest, country title, capital label and body on the same left inset (32px desktop / 22px mobile). Crest is 96px desktop / 76px mobile over the image. Current set: Leonia / Rad Ar Hal / dragon crest v3; Norghard / Mar Nab Mir / crest v1; Linrenet / Ren Um Ga / crest v1. Preserve the raised left box/right large map, crossfade, and top-level header. Country flags are anchored to verified capital coordinates; do not claim full polygon selection is implemented.

- 2026-09-16 H3 revision supersedes the first placement: desktop atlas is large on the RIGHT, starting alongside the headline; city details are LEFT-aligned in a raised bordered box above the map layer, bottom inset 56px. Desktop has no large-map CTA; retain it only on small mobile screens. Map place hover/focus/tap switches the boxed image/copy with a 450ms crossfade; respect reduced motion. Three verified v62 place coordinates currently drive the preview. Header is portaled to body so no hero stacking context can place newcomer text over navigation; modal remains above header.

- 2026-09-16: H3 exploration is now inserted for user review: heading/copy left and atlas CTA right, portrait-ratio original map left and wide verified place scenery right, generous gaps, replayable Reveal. Mobile stacks the images without cropping. Use active v62 map and current-mapped Rad Ar Hal labeled vista (see `public/assets/explore/provenance.json`); never substitute the generated concept map. CTA currently opens a read-only enlarged atlas, not country filters or destination pages. Preserve completed hero and newcomer sections during H3 adjustments.

- Header refinement: reduce only the header image logo to 80% of its previous size (136px desktop, 102.4px mobile). Align desktop navigation toward the right beside search, with a subtle vertical divider and space between navigation and search. Preserve the centered hero wordmark and mobile search/menu usability.

- Move the original gold VIRETH text wordmark into the centered hero group above its slogan, and shift that whole group slightly down (16px desktop/tablet, 12px mobile). Replace the header wordmark with the supplied latest logo image. On the white scrolled header, blacken only its English upper wordmark and preserve the pink lower lettering; keep the original image file unchanged.

- Add a small gold accent above the left edge of the newcomer slogan without moving its text. Use the supplied latest `비레스 로고.png` as a left-aligned background image sized to the section's full height, opacity 0.30, behind the readability gradients and character. Preserve its original aspect ratio, clip overflow inside the section, and do not expand the section to fit the logo.

- Mirror only the background Duran character horizontally so his gaze and open-hand gesture face the left-side introduction. Preserve the source image, background placement, crop, gradients and reveal motion; use CSS reflection.

- Latest Duran correction supersedes the separate-column/mobile-stacked proposal: preserve the original newcomer section height, padding, left-aligned text and city-edge composition. Place the v2 cutout absolutely inside a clipped background div, behind the text, showing only the upper body from the waist up. It must not take up layout space or enlarge the section. Use a dark readability gradient, keep the 24px / 700ms replayable reveal and reduced-motion support. Preserve the source PNG; never use the older hood-up v1 candidates.

- Latest adjustment supersedes the faster-firefly request: restore the initial leisurely 16–28 second flight cycle, preserving current particle count, larger sizes, wider trajectories and glow. Hero CTAs are compact (232×54 desktop, 224×54 mobile), with subtle gold/champagne border glow, one passing sheen, 2px lift and 4px arrow movement on mouse hover; provide keyboard-focus feedback and respect reduced motion.

- Follow-up: fireflies must be more visibly mobile (wider/faster drift) and slightly larger than their first implementation. The encyclopedia hero CTA is an equal-sized full button with the inverse palette of the dark world-exploration CTA: ivory fill and dark navy text. Keep both CTA dimensions equal on mobile too.

- Add subtle warm-gold fireflies around the river and city, with slow individual drift and gentle brightness changes. Keep the title/button corridor clear; reduce density on mobile. Keep the approved background files unchanged. Honor reduced motion and pause off-screen/background-tab animation.

- Foreground objects reveal with a subtle 24px slide/fade up, about 700ms, and short staggered delays. Replay whenever objects leave the viewport completely and re-enter. Honor reduced motion and preserve keyboard access.
- Keep the hero bottom darker with a navy gradient using multiply blending over the scenic background, not over UI copy.
- Header stays fixed at viewport top. At the page top it is transparent with the original light/gold text; after scrolling down it becomes white with black text. Restore the transparent treatment on return to the top. Apply the light theme to the mobile menu as well.

Run the local server yourself and open the preview in the browser available to this environment. Do not give the user server-start instructions when you can run it.

Before making substantial visual changes, use the Product Design plugin's `get-context` skill when the visual source is unclear or no longer matches the current goal. When the user gives durable prototype-specific design feedback, preferences, or decisions, record them in `AGENTS.md`.

When implementing from a selected generated mock, treat that image as the source of truth for layout, component anatomy, density, spacing, color, typography, visible content, and hierarchy.

Build app UI in `src/`. Keep `.openai/hosting.json`, `worker/index.js`, `scripts/prepare-sites-build.mjs`, and `tests/sites-worker.test.mjs` intact so the same local prototype can be handed to Sites. Before a Sites handoff, run `npm run build` and `npm run test:sites`; the build must leave `dist/client/index.html`, `dist/server/index.js`, and `dist/.openai/hosting.json`.
